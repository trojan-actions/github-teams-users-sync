{
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "scripts": {
    "build": "ncc build index.js --out dist --license licenses.txt --minify"
  }
}